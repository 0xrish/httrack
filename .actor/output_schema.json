{
  "actorOutputSchemaVersion": 1,
  "title": "HTTrack Website Scraper Output",
  "properties": {
    "zipFile": {
      "type": "string",
      "title": "ZIP Archive (Key-Value Store)",
      "description": "Download the scraped website as a ZIP file. Check OUTPUT for direct download URL or browse keys below.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/keys"
    },
    "output": {
      "type": "string",
      "title": "Output (with ZIP URL)",
      "description": "View output JSON containing ZIP filename and direct download URL",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
    },
    "dataset": {
      "type": "string",
      "title": "Dataset",
      "description": "View scraping statistics and metadata",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
