{
  "actorOutputSchemaVersion": 1,
  "title": "HTTrack Website Scraper Output",
  "properties": {
    "zipFile": {
      "type": "string",
      "title": "ZIP Archive (Key-Value Store)",
      "description": "Download the scraped website as a ZIP file from key-value store",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/keys/{{zipFile}}"
    },
    "dataset": {
      "type": "string",
      "title": "Dataset",
      "description": "View scraping statistics and metadata",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
