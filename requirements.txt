# Pin pydantic to version 2.3.x to avoid crawlee compatibility issues
# This is a known workaround for the "default and default_factory" bug in crawlee
pydantic >= 2.3.0, < 2.4.0

# Apify SDK - required for Actor development
# Use specific compatible versions
apify >= 2.1.0, < 3.0.0
apify-client >= 1.7.0, < 2.0.0
apify-shared >= 1.1.0, < 2.0.0

# HTTP requests and web scraping utilities (optional, for future use)
requests >= 2.31.0
beautifulsoup4 >= 4.12.0

# Type hints
typing-extensions >= 4.5.0
